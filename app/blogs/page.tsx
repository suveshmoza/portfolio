import { Metadata } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: "Suvesh Blog's",
	description: 'Blogs Wriiten by Suvesh Moza.',
};

export default function Blogs({}: Props) {
	return <p>Coming Soon...</p>;
}
